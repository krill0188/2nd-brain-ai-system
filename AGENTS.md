# AGENTS.md — AI 에이전트 공통 규칙

이 파일은 OpenCode, Codex 등 모든 코딩 에이전트가 따라야 하는 공통 규칙을 정의한다.

## 기본 원칙

1. **한국어로 응답한다.** 코드는 영어, 설명은 한국어.
2. **사용자를 "마스터"라고 부른다.**
3. **기존 파일 구조를 존중한다.** 수정 전 관련 파일을 먼저 읽는다.
4. **임의 삭제 금지.** 파일 삭제 전 반드시 마스터에게 확인한다.
5. **비밀정보 출력 금지.** API 키, 토큰, 비밀번호를 절대 출력하거나 파일에 기록하지 않는다.

## 작업 규칙

6. **테스트 후 결과를 보고한다.** 검증 없이 완료 선언 금지.
7. **작업 범위를 벗어난 수정 금지.** 요청 범위 밖의 파일은 건드리지 않는다.
8. **대규모 변경 전 계획을 먼저 제시한다.** 마스터 승인 후 실행.
9. **비용이 발생하는 API 호출 전 반드시 승인을 받는다.**
10. **Git commit과 push는 명시적 승인 후에만 수행한다.**

## 금지 사항

- `.env`, `.env.*`, 인증 파일 내용을 읽거나 출력하는 것
- OpenRouter 자동충전 활성화
- 유료 API를 무제한 반복 호출하는 것
- 저장소 전체를 불필요하게 반복 스캔하는 것
- 같은 명령을 무한 반복하는 것

## 모델 역할 구분

| 도구 | 주요 용도 | 실행 명령 |
|---|---|---|
| OpenCode + Kimi | 문서 정리, 코드 초안, 반복 작업 | `opencode` |
| Claude Code | 아키텍처, 복잡한 디버깅, 설계 검토 | `claude` |
| Codex | 코드 구현, 버그 수정, 기능 추가 | `codex` |
